#import { Module, TokenBalance } into PriceResolver from "ens/interface.price.resolvers.defiwrapper.eth"
#import { Module, Token } into TokenResolver from "ens/interface.token.resolvers.defiwrapper.eth"
#import { Module } into Coingecko from "ens/coingecko.defiwrapper.eth"
#import { Module } into Ethereum from "ens/ethereum.web3api.eth"

#use { getImplementations } for TokenResolver

type Module implements PriceResolver_Module {}
