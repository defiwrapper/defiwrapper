{
  "name": "@defiwrapper/core-sdk-resolvers",
  "description": "Core SDK Resolvers",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "build": "yarn build:token && yarn build:account && yarn build:price && yarn build:asset && yarn build:protocol ",
    "build:token": "lerna run build --scope @defiwrapper/token-resolvers",
    "build:price": "lerna run build --scope @defiwrapper/price-resolvers",
    "build:account": "lerna run build --scope @defiwrapper/account-resolvers",
    "build:asset": "lerna run build --scope @defiwrapper/asset-resolvers",
    "build:protocol": "lerna run build --scope @defiwrapper/protocol-resolvers"
  }
}
