{
  "name": "@defiwrapper/defi-sdk",
  "description": "Defi SDK",
  "private": true,
  "version": "2.0.1",
  "scripts": {
    "build": "yarn build:resolvers && yarn build:interface && yarn build:sdk",
    "build:interface": "lerna run build --scope @defiwrapper/core-sdk-interface",
    "build:resolvers": "lerna run build --scope @defiwrapper/core-sdk-resolvers",
    "build:sdk": "lerna run build --scope @defiwrapper/core-sdk"
  },
  "devDependencies": {
    "dotenv": "^16.0.2"
  }
}
