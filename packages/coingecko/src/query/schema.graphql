#import { Query } into HTTP from "w3://ens/http.web3api.eth"
#import { Ping, supportedCurrencies } from "../commons/schema.graphql"

type Query {
  ping: Ping!
}

type Query{
  supportedCurrencies: supportedCurrencies!
}

