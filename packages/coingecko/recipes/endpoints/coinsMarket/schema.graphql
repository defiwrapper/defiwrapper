query coinsMarkets (
    $vs_currency: String!,
    $id: String,
    $category: String,
    $order: String,
    $per_page: Int,
    $page: Int,
    $sparkline: Boolean,
    $price_change_percentage: String
  ){
  coinsMarkets(
    vs_currency: $vs_currency
  )
}
    
