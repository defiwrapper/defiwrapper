{
  "name": "@defiwrapper/account-resolvers",
  "description": "Account Resolvers",
  "private": true,
  "version": "0.0.1-prealpha.70",
  "scripts": {
    "build": "yarn build:interface && yarn build:resolvers",
    "build:interface": "lerna run build --scope @defiwrapper/account-resolver-interface",
    "build:resolvers": "lerna run build --scope @defiwrapper/*-account-resolver",
    "test": "yarn test:resolvers",
    "test:resolvers": "lerna run test --scope @defiwrapper/*-account-resolver"
  }
}
