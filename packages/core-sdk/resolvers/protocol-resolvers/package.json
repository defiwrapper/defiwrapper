{
  "name": "@defiwrapper/protocol-resolvers",
  "description": "Protocol Resolvers",
  "private": true,
  "version": "0.0.1-prealpha.64",
  "scripts": {
    "build": "yarn build:interface && yarn build:resolvers",
    "build:interface": "lerna run build --scope @defiwrapper/protocol-resolver-interface",
    "build:resolvers": "lerna run build --scope @defiwrapper/*-protocol-resolver"
  }
}
