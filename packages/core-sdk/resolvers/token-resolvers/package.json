{
  "name": "@defiwrapper/token-resolvers",
  "description": "Token Resolvers",
  "private": true,
  "version": "0.0.1-prealpha.69",
  "scripts": {
    "build": "yarn build:interface && yarn build:resolvers",
    "build:interface": "lerna run build --scope @defiwrapper/token-resolver-interface",
    "build:resolvers": "lerna run build --scope @defiwrapper/*-token-resolver"
  }
}
