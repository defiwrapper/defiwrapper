#import { Query, Connection } into Ethereum from "w3://ens/ethereum.web3api.eth"
#import { Query, TokenComponent, Token } into Interface from "w3://ens/interface.token-resolvers.defiwrapper.eth"
#import { Query, TokenType } into Token from "w3://ens/token.defiwrapper.eth"

type Query implements Interface_Query {}

type QueryEnv {
  connection: Ethereum_Connection!
}
