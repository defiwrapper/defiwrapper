#import { Query, Connection } into Ethereum from "w3://ens/ethereum.web3api.eth"
#import { Query } into TokenResolver from "w3://ens/interface.token.resolvers.defiwrapper.eth"

type Query implements TokenResolver_Query {}

type QueryEnv {
  connection: Ethereum_Connection!
}
