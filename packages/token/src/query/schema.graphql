#import { Query, Connection } into Ethereum from "w3://ens/ethereum.web3api.eth"
#import { Token } from "../../../core-types-interface/build/schema.graphql"

type Query {
  getToken(
    address: String!,
    type: TokenType!
  ): Token
}

type QueryEnv {
  connection: Ethereum_Connection!
}

enum TokenType {
  ERC20
}