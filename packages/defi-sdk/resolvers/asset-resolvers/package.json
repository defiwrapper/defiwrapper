{
  "name": "@defiwrapper/asset-resolvers",
  "description": "Asset Resolvers",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "build": "yarn build:interface && yarn build:resolvers",
    "build:interface": "lerna run build --scope @defiwrapper/asset-resolver-interface",
    "build:resolvers": "lerna run build --scope @defiwrapper/*-asset-resolver",
    "test": "yarn test:resolvers",
    "test:resolvers": "lerna run test --scope @defiwrapper/*-asset-resolver"
  }
}
