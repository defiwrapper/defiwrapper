#import { Module, Connection } into Ethereum from "wrap://ens/ethereum.polywrap.eth"
#import { Module } into TokenResolver from "wrap://ens/interface.token.resolvers.defiwrapper.eth"

type Module implements TokenResolver_Module {}

type Env {
  connection: Ethereum_Connection!
}
