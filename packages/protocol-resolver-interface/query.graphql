type Token {
  address: String!
  name: String!
  symbol: String!
  decimals: Int!
  totalSupply: BigInt!
}

type Protocol {
  organization: String!
  name: String!
  version: String
  forkedFrom: Protocol
}

type Query {
  getProtocol(
    token: Token!
  ): Protocol!
}